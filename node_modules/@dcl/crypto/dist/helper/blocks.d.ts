import type { BlockIdentifier } from 'eth-connect';
import type RequestManager from 'eth-connect';
export interface SavedBlock {
    number: number;
    timestamp: number;
}
export interface BlockResponse {
    block: number;
    timestamp: number;
}
export default class Blocks {
    private requestManager;
    checkedBlocks: {
        [key: string]: number[];
    };
    saveBlocks: boolean;
    savedBlocks: {
        [key: string]: SavedBlock;
    };
    requests: number;
    blockTime?: number;
    firstTimestamp?: number;
    constructor(requestManager: RequestManager, save?: boolean);
    fillBlockTime(): Promise<{
        blockTime: number;
        firstTimestamp: number;
    }>;
    getDate(date: number, after?: boolean): Promise<BlockResponse>;
    findBetter(date: number, predictedBlock: SavedBlock, after: boolean, blockTime?: number): Promise<number>;
    isBetterBlock(date: number, predictedBlock: SavedBlock, after: boolean): Promise<boolean>;
    getNextBlock(date: number, currentBlock: number, skip: number): number;
    getBlockWrapper(block: BlockIdentifier): Promise<SavedBlock>;
}
//# sourceMappingURL=blocks.d.ts.map