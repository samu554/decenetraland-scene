import type { Contract } from 'eth-connect';
import type RequestManager from 'eth-connect';
export type SignatureValidator = Contract & {
    isValidSignature(hash: Uint8Array, signature: Uint8Array, block?: string | number): Promise<Uint8Array>;
};
export declare function SignatureValidator(requestManager: RequestManager, address: string): Promise<SignatureValidator>;
//# sourceMappingURL=SignatureValidator.d.ts.map