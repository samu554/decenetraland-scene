import { Filter } from './Filters/types';
import type { AssetNode, AssetNodeFolder } from './types';
export declare const ROOT = "File System";
export type TreeNode = Omit<AssetNode, 'children'> & {
    children?: string[];
    matches?: string[];
};
interface TreeGenerationResult {
    tree: Map<string, TreeNode>;
    filters: Set<Filter>;
}
export declare const generateAssetTree: (folders: AssetNodeFolder[], openNodes: Set<string>, searchTerm?: string, activeFilter?: Filter) => TreeGenerationResult;
export declare const getChildren: (val: string, tree: Map<string, TreeNode>, searchTerm: string, activeFilter: Filter) => string[];
export declare const getTiles: (nodeId: string, tree: Map<string, TreeNode>, searchTerm: string, activeFilter: Filter) => TreeNode[];
export {};
