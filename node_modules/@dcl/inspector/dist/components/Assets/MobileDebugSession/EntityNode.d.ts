interface EntityNodeProps {
    eid: number;
    entities: Record<number, {
        components: Record<string, unknown>;
        parent: number;
    }>;
    childrenMap: Record<number, number[]>;
    depth: number;
    selectedEid: number | null;
    onSelect: (eid: number) => void;
    matchesFilter: (eid: number) => boolean;
}
declare function EntityNode({ eid, entities, childrenMap, depth, selectedEid, onSelect, matchesFilter, }: EntityNodeProps): import("react/jsx-runtime").JSX.Element | null;
export default EntityNode;
