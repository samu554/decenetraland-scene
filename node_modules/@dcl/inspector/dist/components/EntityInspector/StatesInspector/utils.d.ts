import type { StatesInput } from './types';
export declare function sanitize(states: string[]): string[];
export declare function isValidInput(inputs: StatesInput): boolean;
export declare function getUniqueState(newState: string, states: string[], attempt?: number): string;
export declare function isRepeated(state: string, states: string[]): boolean;
export declare const fromStates: (value: StatesInput) => string[];
export declare const toStates: (items: string[], currentValue: StatesInput) => StatesInput;
