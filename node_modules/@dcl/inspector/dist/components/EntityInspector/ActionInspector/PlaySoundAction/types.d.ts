import type { ActionPayload, ActionType } from '@dcl/asset-packs';
export interface Props {
    value: Partial<ActionPayload<ActionType.PLAY_SOUND>>;
    onUpdate: (value: ActionPayload<ActionType.PLAY_SOUND>) => void;
}
export declare enum PLAY_MODE {
    PLAY_ONCE = "play-once",
    LOOP = "loop"
}
export declare const PLAY_MODE_OPTIONS: {
    label: string;
    value: PLAY_MODE;
}[];
