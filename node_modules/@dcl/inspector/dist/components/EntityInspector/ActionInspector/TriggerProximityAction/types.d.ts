import type { ActionPayload, ActionType } from '@dcl/asset-packs';
import { ProximityLayer } from '@dcl/asset-packs';
export interface Props {
    value: Partial<ActionPayload<ActionType.DAMAGE>>;
    onUpdate: (value: ActionPayload<ActionType.DAMAGE>) => void;
}
export declare const LayerOptions: {
    value: ProximityLayer;
    label: string;
}[];
