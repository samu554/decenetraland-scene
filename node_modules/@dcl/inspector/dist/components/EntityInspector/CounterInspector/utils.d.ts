import type { Counter } from '@dcl/asset-packs';
import type { CounterInput } from './types';
export declare function fromCounter(counter: Counter): CounterInput;
export declare function toCounter(input: CounterInput): Counter;
export declare function isValidInput(inputs: CounterInput): boolean;
