import type { Entity } from '@dcl/ecs';
export declare enum LightKind {
    POINT = "point",
    SPOT = "spot"
}
export declare const LIGHT_TYPE_OPTIONS: {
    label: string;
    value: LightKind;
}[];
export type LightInput = {
    type: LightKind;
    active: boolean;
    color: string;
    intensity: string;
    shadow: boolean;
    shadowMaskSrc?: string;
    range?: string;
    spot?: {
        innerAngle: string;
        outerAngle: string;
    };
};
export type Props = {
    entities: Entity[];
    initialOpen?: boolean;
};
