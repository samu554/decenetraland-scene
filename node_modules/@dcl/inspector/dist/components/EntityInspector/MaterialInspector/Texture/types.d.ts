import { TextureFilterMode, TextureWrapMode } from '@dcl/ecs';
import type { useComponentInput } from '../../../../hooks/sdk/useComponentInput';
import type { AssetCatalogResponse } from '../../../../lib/data-layer/remote-data-layer';
import type { TextureType } from '../types';
export type VideoTexture = Map<number, {
    name: string;
}>;
export type Props = {
    label: string;
    texture: TextureType;
    files?: AssetCatalogResponse;
    getInputProps: ReturnType<typeof useComponentInput>['getInputProps'];
};
export declare enum Texture {
    TT_TEXTURE = "texture",
    TT_AVATAR_TEXTURE = "avatarTexture",
    TT_VIDEO_TEXTURE = "videoTexture"
}
export type TextureInput = {
    type: Texture;
    src?: string;
    userId?: string;
    videoPlayerEntity?: string;
    wrapMode: string;
    filterMode: string;
    offset?: {
        x: string;
        y: string;
    };
    tiling?: {
        x: string;
        y: string;
    };
};
export declare const WRAP_MODES: {
    value: TextureWrapMode;
    label: string;
}[];
export declare const FILTER_MODES: {
    value: TextureFilterMode;
    label: string;
}[];
export declare const TEXTURE_TYPES: {
    value: any;
    label: string;
}[];
