import type { Material } from '@babylonjs/core';
import { type Metrics } from './types';
export declare function getSceneLimits(parcels: number): Metrics;
/**
 * Extracts textures from a material and adds them to the textures set.
 * Materials can have multiple texture properties (albedoTexture, normalTexture, etc.)
 */
export declare function collectTexturesFromMaterial(material: Material, texturesSet: Set<string>, ignoreTextures: string[]): void;
