import type { Entity } from '@dcl/ecs';
import type { AssetData } from '../../lib/logic/catalog';
export declare const useCustomAsset: () => {
    create: (entities: Entity | Entity[]) => {
        composite: AssetData["composite"];
        resources: string[];
    } | undefined;
};
export default useCustomAsset;
