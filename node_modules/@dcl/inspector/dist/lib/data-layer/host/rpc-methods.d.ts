import type { IEngine, OnChangeFunction } from '@dcl/ecs';
import type { DataLayerRpcServer, FileSystemInterface } from '../types';
export declare function initRpcMethods(fs: FileSystemInterface, engine: IEngine, addEngineListener: (fn: OnChangeFunction) => void): Promise<DataLayerRpcServer & {
    engine: IEngine;
}>;
