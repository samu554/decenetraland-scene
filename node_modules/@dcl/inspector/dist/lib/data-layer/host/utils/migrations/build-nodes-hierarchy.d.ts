import type { IEngine } from '@dcl/ecs';
import type { Node } from '../../../../sdk/components';
/**
 * Build Node component hierarchy from current engine status
 * @param engine engine to build upon
 */
export declare function buildNodesHierarchy(engine: IEngine): Node[];
export declare function buildNodesHierarchyIfNotExists(engine: IEngine): void;
