import type { FileSystemInterface } from '../types';
/**
 * Recursively collects files from a directory, applying ignore patterns similar to dclignore.
 * This implementation uses the 'ignore' library to match the DCL ignore behavior exactly.
 *
 * @param fs - File system interface
 * @param dirPath - Directory path to scan
 * @param files - Array to accumulate file paths
 * @param recursive - Whether to recurse into subdirectories
 * @param ignorePatterns - Array of .gitignore-style patterns to ignore
 * @returns Promise resolving to array of file paths
 */
export declare function getFilesInDirectory(fs: FileSystemInterface, dirPath: string, files: string[], recursive?: boolean, ignorePatterns?: string[]): Promise<string[]>;
export declare const DIRECTORY: {
    ASSETS: string;
    SCENE: string;
    THUMBNAILS: string;
    CUSTOM: string;
    ASSET_PACKS: string;
};
export declare const EXTENSIONS: string[];
export declare function withAssetDir(filePath?: string): string;
export declare function withAssetPacksDir(filePath: string): string;
export declare function isFileInAssetDir(filePath?: string): boolean;
export declare function getCurrentCompositePath(): string;
export declare function transformBinaryToBase64Resource(content: Uint8Array): string;
export declare function transformBase64ResourceToBinary(base64Resource: string): Buffer;
