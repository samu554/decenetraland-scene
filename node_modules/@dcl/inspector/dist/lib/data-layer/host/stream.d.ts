import type { DataLayerContext } from '../types';
export declare function createStream(stream: AsyncIterable<{
    data: Uint8Array;
}>, ctx: Omit<DataLayerContext, 'fs'>): AsyncGenerator<{
    data: Uint8Array;
}>;
