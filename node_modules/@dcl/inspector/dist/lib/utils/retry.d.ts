type RetryOptions = {
    retries?: number;
    delay?: number;
};
export declare function retry<T>(fn: (...args: any[]) => Promise<T>, args: any[], { retries, delay }?: RetryOptions): Promise<T>;
export {};
