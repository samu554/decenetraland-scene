import type { Transport } from '@dcl/mini-rpc';
import { RPC } from '@dcl/mini-rpc';
import type { RootState } from '../../../redux/store';
import { Method, type Params, type Result } from './types';
export declare class SceneMetricsServer extends RPC<Method, Params, Result> {
    constructor(transport: Transport, store: {
        getState: () => RootState;
    });
}
