import type { Scene } from '@babylonjs/core';
import { Vector3, TransformNode, UtilityLayerRenderer } from '@babylonjs/core';
import type { EcsEntity } from '../EcsEntity';
import type { IGizmoTransformer } from './types';
import { GizmoType } from './types';
interface GizmoManagerInterface {
    calculateCentroid: () => Vector3;
}
export declare class FreeGizmo implements IGizmoTransformer {
    private scene;
    private utilityLayer;
    type: GizmoType;
    private selectedEntities;
    private isDragging;
    private snapDistance;
    private isWorldAligned;
    private pivotPosition;
    private lastSnappedPivotPosition;
    private entityOffsets;
    private dragPlanePosition;
    private gizmoIndicator;
    private onDragEndCallback;
    private updateEntityPosition;
    private dispatchOperations;
    private gizmoManager;
    constructor(scene: Scene, utilityLayer?: UtilityLayerRenderer);
    setup(): void;
    enable(): void;
    cleanup(): void;
    setEntities(entities: EcsEntity[]): void;
    setUpdateCallbacks(updateEntityPosition: (entity: EcsEntity) => void, dispatchOperations: () => void): void;
    setWorldAligned(value: boolean): void;
    setSnapDistance(distance: number): void;
    setOnDragEndCallback(callback: () => void): void;
    setGizmoManager(gizmoManager: GizmoManagerInterface): void;
    onDragStart(entities: EcsEntity[], _gizmoNode: TransformNode): void;
    update(entities: EcsEntity[], _gizmoNode: TransformNode): void;
    onDragEnd(): void;
    dispose(): void;
    private resetState;
    private resetDragState;
    private setupSceneObservers;
    private removeSceneObservers;
    private handleDrag;
    private handleDragEnd;
    private updatePivotPosition;
    private shouldMoveEntities;
    private moveEntitiesToPivot;
    private getSnappedPivotPosition;
    private initializePivotPosition;
    private initializeEntityOffsets;
    /** Manual drag implementation: raycast against a horizontal plane at the drag start Y position.
     * This approach is necessary because PointerDragBehavior cannot be attached after pointer down.
     * Raycasting against a plane (instead of full scene picking) maintains performance while
     * providing accurate cursor-to-world position mapping regardless of camera angle.
     */
    private calculateDragDelta;
    /** Custom picking with predicate to only consider selected entities' meshes.
     * By filtering to only selected entities,
     * we ensure the user can interact with the currently selected entity even if others overlap.
     */
    private findClickedEntityFromSelected;
    private startDrag;
    private applyWorldPositionToEntity;
    private updateEntityTransform;
    private getCentroid;
    private createGizmoIndicator;
    private createCrossMesh;
    private createRedMaterial;
    private createBlueMaterial;
    updateGizmoIndicator(): void;
    private removeGizmoIndicator;
}
export {};
