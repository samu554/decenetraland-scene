import type { GizmoManager } from '@babylonjs/core';
import { Vector3, TransformNode } from '@babylonjs/core';
import type { EcsEntity } from '../EcsEntity';
import type { IGizmoTransformer } from './types';
import { GizmoType } from './types';
export declare class PositionGizmo implements IGizmoTransformer {
    private gizmoManager;
    private snapPosition;
    type: GizmoType;
    private entityStates;
    private pivotPosition;
    private isDragging;
    private currentEntities;
    private updateEntityPosition;
    private dispatchOperations;
    private isWorldAligned;
    private parentMatrixCache;
    private dragStartObserver;
    private dragObserver;
    private dragEndObserver;
    constructor(gizmoManager: GizmoManager, snapPosition: (position: Vector3) => Vector3);
    setup(): void;
    private applyPlanarGizmoOffsets;
    enable(): void;
    cleanup(): void;
    setEntities(entities: EcsEntity[]): void;
    setUpdateCallbacks(updateEntityPosition: (entity: EcsEntity) => void, dispatchOperations: () => void): void;
    setWorldAligned(value: boolean): void;
    setSnapDistance(distance: number): void;
    private getPositionGizmo;
    private resetState;
    private updateGizmoAlignment;
    private resetGizmoRotation;
    private syncGizmoRotationWithEntity;
    private getEntityWorldRotation;
    private setupDragObservables;
    private cleanupDragObservables;
    private updateEntitiesInRealTime;
    onDragStart(entities: EcsEntity[], _gizmoNode: TransformNode): void;
    private calculatePivotPosition;
    private storeEntityStates;
    update(entities: EcsEntity[], gizmoNode: TransformNode): void;
    private updateEntityTransform;
    private applyLocalPosition;
    private applyWorldPosition;
    private applyTransforms;
    private calculateCentroid;
    onDragEnd(): void;
    private syncGizmoWithFinalPositions;
    private resetDragState;
}
