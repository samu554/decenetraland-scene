import type { GizmoManager } from '@babylonjs/core';
import { Vector3, TransformNode } from '@babylonjs/core';
import type { EcsEntity } from '../EcsEntity';
import type { IGizmoTransformer } from './types';
import { GizmoType } from './types';
export declare class ScaleGizmo implements IGizmoTransformer {
    private gizmoManager;
    private snapScale;
    private getUniformScaleOnly?;
    type: GizmoType;
    private initialOffsets;
    private initialScales;
    private initialRotations;
    private initialPositions;
    private pivotPosition;
    private initialGizmoScale;
    private isDragging;
    private dragStartObserver;
    private dragObserver;
    private dragEndObserver;
    private currentEntities;
    private updateEntityScale;
    private dispatchOperations;
    private isWorldAligned;
    private readonly scaleSensitivity;
    private readonly minScaleValue;
    private readonly positiveSensitivity;
    private readonly negativeSensitivity;
    private readonly negativeThreshold;
    private planeCubesCreated;
    private planeCubeMeshes;
    private planeMaterials;
    private activelyDraggingPlane;
    private planePointerObservers;
    private isDraggingUniformScale;
    private initialUniformScaleMousePos;
    private uniformScalePointerObservers;
    private uniformScaleCenterMesh;
    constructor(gizmoManager: GizmoManager, snapScale: (scale: Vector3) => Vector3, getUniformScaleOnly?: (() => boolean) | undefined);
    private clampScaleComponent;
    private clampScale;
    /**
     * Calculate scale factor based on drag distance.
     * Maps drag distance to scale factor with asymmetric sensitivity:
     * - Origin (dragDistance = 0) → scaleFactor = 1.0 (original size)
     * - Positive drag (right/up) → scaleFactor > 1.0 (growing)
     * - Small negative drag (left/down) → scaleFactor < 1.0 but > 0 (shrinking, stays positive)
     * - Large negative drag → scaleFactor can go negative (requires pronounced drag)
     */
    private calculateScaleFactor;
    setup(): void;
    private createPlaneCubes;
    private addPlaneHoverBehavior;
    private static readonly UNIFORM_CUBE_EMISSIVE_DEFAULT;
    private static readonly UNIFORM_CUBE_EMISSIVE_HOVER;
    private addUniformScaleDragBehavior;
    private addPlaneDragBehavior;
    private setPlaneMaterialsAppearance;
    private setBuiltInGizmoAppearance;
    private configureUniformScaleGizmo;
    enable(): void;
    cleanup(): void;
    setEntities(entities: EcsEntity[]): void;
    setUpdateCallbacks(updateEntityScale: (entity: EcsEntity) => void, dispatchOperations: () => void): void;
    setWorldAligned(_value: boolean): void;
    setSnapDistance(distance: number): void;
    private syncGizmoAlignment;
    private setupDragObservables;
    private cleanupDragObservables;
    onDragStart(entities: EcsEntity[], gizmoNode: TransformNode): void;
    update(entities: EcsEntity[], gizmoNode: TransformNode): void;
    /**
     * Apply scale transformation to an entity based on scale change from pivot.
     * Handles both entities with and without parents.
     */
    private applyScaleTransform;
    onDragEnd(): void;
    private clearInitialState;
}
