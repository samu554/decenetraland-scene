import type { Scene, TransformNode, AbstractMesh, LinesMesh } from '@babylonjs/core';
import { StandardMaterial, Color3, Mesh, Vector3 } from '@babylonjs/core';
export declare function createSpawnPointMaterial(name: string, scene: Scene, color: Color3, alpha?: number): StandardMaterial;
export declare function createAvatarPlaceholderAsync(name: string, scene: Scene, parent: TransformNode): Promise<Mesh>;
/**
 * Creates a ground plane mesh showing the random offset area.
 * The offset extends equally in +/- X and +/- Z, forming a rectangular area.
 */
export declare function createOffsetArea(name: string, scene: Scene, parent: TransformNode, offsetX: number, offsetZ: number): Mesh;
/**
 * Creates a small cube representing the camera target position.
 * Parented to a stable node (not the spawn point root) so it stays in place
 * when the user drags the avatar with the gizmo.
 */
export declare function createCameraTargetCube(name: string, scene: Scene, parent: TransformNode, targetPosition: Vector3): Mesh;
export declare function setSpawnPointSelected(avatarMesh: Mesh, selected: boolean): void;
export declare function setOffsetAreaSelected(areaMesh: Mesh, selected: boolean): void;
export declare function setCameraTargetSelected(cameraTargetMesh: Mesh, selected: boolean): void;
export declare function isCameraTargetMesh(mesh: AbstractMesh): boolean;
export declare function isSpawnPointMesh(mesh: AbstractMesh): boolean;
/** Wireframe cube centered on the parent mesh, shown when camera target exits bounds */
export declare function createCameraTargetOutOfBoundsIndicator(name: string, scene: Scene, parent: TransformNode): LinesMesh;
export declare function getSpawnPointIndexFromMesh(mesh: AbstractMesh): number | null;
/** Wireframe box matching the spawn area footprint, shown when spawn point exits bounds */
export declare function createOutOfBoundsIndicator(name: string, scene: Scene, parent: TransformNode, halfExtentX: number, halfExtentZ: number): LinesMesh;
