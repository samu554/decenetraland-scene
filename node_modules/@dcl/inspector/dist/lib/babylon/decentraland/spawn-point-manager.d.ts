import type { Scene, Mesh, LinesMesh } from '@babylonjs/core';
import { TransformNode } from '@babylonjs/core';
import type { SceneSpawnPoint } from '../../sdk/components';
import { isSpawnPointMesh, isCameraTargetMesh, getSpawnPointIndexFromMesh } from './spawn-point-visuals';
export interface SpawnPointVisual {
    index: number;
    rootNode: TransformNode;
    avatarMesh: Mesh | null;
    areaMesh: Mesh | null;
    cameraTargetMesh: Mesh | null;
    outOfBoundsIndicator: LinesMesh | null;
    cameraTargetOutOfBoundsIndicator: LinesMesh | null;
    offsetX: number;
    offsetZ: number;
}
export type SpawnPointSelectionTarget = 'position' | 'cameraTarget';
type SelectionData = {
    index: number | null;
    target: SpawnPointSelectionTarget;
};
/**
 * Creates a SpawnPointManager that handles visual representation of spawn points.
 * Follows the layout-manager.ts pattern (memoized per Babylon scene).
 */
export declare const createSpawnPointManager: (a: Scene, ...params: never[]) => {
    updateFromSceneComponent: (spawnPoints: readonly SceneSpawnPoint[] | undefined) => void;
    selectSpawnPoint: (index: number | null) => void;
    selectCameraTarget: (index: number) => void;
    getSelectedIndex: () => number | null;
    getSelectedTarget: () => SpawnPointSelectionTarget;
    isMeshSpawnPoint: typeof isSpawnPointMesh;
    isMeshCameraTarget: typeof isCameraTargetMesh;
    findSpawnPointByMesh: typeof getSpawnPointIndexFromMesh;
    getSpawnPointNode: (index: number) => TransformNode | null;
    getCameraTargetNode: (index: number) => TransformNode | null;
    setSpawnPointVisible: (index: number, name: string, visible: boolean) => void;
    isSpawnPointHidden: (name: string) => boolean;
    setSpawnPointOutOfBoundsVisible: (index: number, visible: boolean) => void;
    setCameraTargetOutOfBoundsVisible: (index: number, visible: boolean) => void;
    getSpawnAreaHalfExtents: (index: number) => {
        x: number;
        z: number;
    } | null;
    onSelectionChange: (cb: (data: SelectionData) => void) => () => void;
    onVisibilityChange: (cb: (data: {
        index: number;
        name: string;
        visible: boolean;
    }) => void) => () => void;
    dispose: () => void;
};
export type SpawnPointManager = ReturnType<typeof createSpawnPointManager>;
export {};
