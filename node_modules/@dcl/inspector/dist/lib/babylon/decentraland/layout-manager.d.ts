import type { AbstractMesh, Scene } from '@babylonjs/core';
import { BoundingBox, Vector3 } from '@babylonjs/core';
import type { Layout } from '../../utils/layout';
export declare const getLayoutManager: (a: Scene, ...params: never[]) => {
    getLayout: () => Layout | null;
    setLayout: (_layout: Layout) => boolean;
    getLayoutBoundingBox: () => BoundingBox;
    isEntityOutsideLayout: (mesh: AbstractMesh) => boolean;
    isPositionInBounds: (position: Vector3) => boolean;
};
