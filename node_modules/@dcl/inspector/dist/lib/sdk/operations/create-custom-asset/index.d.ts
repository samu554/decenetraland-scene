import type { Entity, IEngine } from '@dcl/ecs';
import type { AssetData } from '../../../logic/catalog';
export declare function createCustomAsset(engine: IEngine): (entities: Entity[]) => {
    composite: AssetData["composite"];
    resources: string[];
};
export default createCustomAsset;
