import type { Entity, IEngine, Vector3Type } from '@dcl/ecs';
import type { EnumEntity } from '../../enum-entity';
import type { AssetData } from '../../../logic/catalog';
export declare function addAsset(engine: IEngine): (parent: Entity, src: string, name: string, position: Vector3Type, base: string, enumEntityId: EnumEntity, composite?: AssetData["composite"], assetId?: string, custom?: boolean) => Entity;
export default addAsset;
