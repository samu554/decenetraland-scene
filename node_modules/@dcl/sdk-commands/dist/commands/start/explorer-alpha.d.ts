import { Result } from 'arg';
import { args as startArgs } from '.';
import { CliComponents } from '../../components';
export declare function runExplorerAlpha(components: CliComponents, opts: {
    cwd: string;
    realm: string;
    baseCoords: {
        x: number;
        y: number;
    };
    isHub: boolean;
    args: Result<typeof startArgs>;
}): Promise<void>;
