import { EditorComponentsTypes } from '@dcl/inspector';
import { CliComponents } from '../components';
type CompositeComponents = Pick<CliComponents, 'logger' | 'fs'>;
type ScriptItem = EditorComponentsTypes['Script']['value'][number];
export type Script = ScriptItem & {
    entity: number;
};
export declare function getAllComposites(components: CompositeComponents, workingDirectory: string): Promise<{
    watchFiles: string[];
    compositeLines: string[];
    scripts: Map<string, Script[]>;
    withErrors: boolean;
    maxCompositeEntity: number;
}>;
export {};
