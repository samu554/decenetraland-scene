import { GLTFImage, GLTF, ValidationMessage } from "../types";
export declare class ImageValidator {
    private externalResourceFunction?;
    constructor(externalResourceFunction?: (uri: string) => Promise<Uint8Array>);
    validate(image: GLTFImage, index: number, gltf: GLTF): ValidationMessage[];
    private validateMimeTypeExtensionRequirements;
    validateExternalResources(image: GLTFImage, index: number): Promise<ValidationMessage[]>;
    private detectImageFormat;
    private validateImageData;
    private validateJPEGData;
    private validatePNGData;
    private getImageDimensions;
    private getPNGDimensions;
    private getJPEGDimensions;
}
//# sourceMappingURL=image-validator.d.ts.map