import { GLTFNode, GLTF, ValidationMessage } from "../types";
import { UsageTracker } from "../usage-tracker";
export declare class NodeValidator {
    validate(node: GLTFNode, index: number, gltf: GLTF, usageTracker?: UsageTracker): ValidationMessage[];
    private isMatrixDecomposableToTRS;
    private findParentNode;
    private isNodeReferencedBySkin;
}
//# sourceMappingURL=node-validator.d.ts.map