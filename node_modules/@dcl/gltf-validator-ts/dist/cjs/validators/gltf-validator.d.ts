import { GLTF, Issues, Severity, ResourceReference } from "../types";
export interface ValidatorOptions {
    maxIssues?: number;
    ignoredIssues?: string[];
    onlyIssues?: string[];
    severityOverrides?: Record<string, Severity>;
    externalResourceFunction?: (uri: string) => Promise<Uint8Array>;
}
export declare class GLTFValidator {
    private options;
    private messages;
    private usageTracker;
    private bufferData;
    constructor(options?: ValidatorOptions);
    validate(gltf: GLTF, isGLB?: boolean, resources?: ResourceReference[]): Promise<{
        issues: Issues;
    }>;
    setBufferData(bufferIndex: number, data: Uint8Array): void;
    getBufferData(bufferIndex: number): Uint8Array | undefined;
    private validateRootProperties;
    private validateCollectionType;
    private validateAsset;
    private validateBuffers;
    private validateBufferViews;
    private validateAccessors;
    private validateAnimations;
    private validateNodes;
    private validateMeshes;
    private validateMaterials;
    private validateTextures;
    private validateImages;
    private validateSamplers;
    private validateCameras;
    private validateScenes;
    private validateDefaultScene;
    private validateSkins;
    private validateAnimationSkinConflicts;
    private validateSkinnedNodeScenePresence;
    private validateSkinnedMeshNodeHierarchy;
    private checkNodeLoops;
    private detectNodeLoop;
    private checkUnusedObjects;
    private validateExtensions;
    private addMessages;
    private isExtensionUsed;
    private addMessage;
    private applyFilters;
    private buildIssues;
    private sortMessagesForTestCompatibility;
    private validateGLB;
    private checkUndeclaredExtensions;
    private escapeJsonPointer;
}
//# sourceMappingURL=gltf-validator.d.ts.map