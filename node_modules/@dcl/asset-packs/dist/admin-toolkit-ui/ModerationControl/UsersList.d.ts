import { type IEngine } from '@dcl/ecs';
import ReactEcs from '@dcl/react-ecs';
import { type SceneBanUser } from './api';
import { type SceneAdmin } from './.';
export declare enum UserListType {
    ADMIN = "admin",
    BAN = "ban"
}
type ModalUserListProps = {
    users: SceneAdmin[] | SceneBanUser[];
    engine: IEngine;
    type: UserListType;
};
export declare function ModalUserList({ users, engine, type }: ModalUserListProps): ReactEcs.JSX.Element;
export {};
