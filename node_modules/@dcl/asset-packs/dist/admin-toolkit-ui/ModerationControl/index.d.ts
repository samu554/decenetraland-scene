import type { IEngine } from '@dcl/ecs';
import ReactEcs from '@dcl/react-ecs';
import { type GetPlayerDataRes } from '../../types';
type Props = {
    engine: IEngine;
    player: GetPlayerDataRes | null | undefined;
    sceneAdmins: SceneAdmin[];
};
export declare const getBtnModerationControl: () => string;
export type SceneAdmin = {
    name?: string;
    address: string;
    role?: 'owner' | 'operator' | 'admin';
    verified?: boolean;
    canBeRemoved: boolean;
};
type State = {
    showModalAdminList?: boolean;
    adminToRemove?: SceneAdmin;
    showModalBanList?: boolean;
    unbanMessage?: string | null;
};
export declare const moderationControlState: State;
export declare function ModerationControl({ engine: _engine, player: _player, sceneAdmins }: Props): ReactEcs.JSX.Element;
export {};
