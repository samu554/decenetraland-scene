import type { IEngine } from '@dcl/ecs';
import ReactEcs from '@dcl/react-ecs';
import { type State } from './types';
export declare function RewardsControl({ engine, state }: {
    engine: IEngine;
    state: State;
}): ReactEcs.JSX.Element;
