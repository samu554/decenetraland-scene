import { IEngine } from '@dcl/ecs';
import ReactEcs, { UiTransformProps } from '@dcl/react-ecs';
interface LoadingProps {
    engine: IEngine;
    width?: number;
    height?: number;
    uiTransform?: UiTransformProps;
}
export declare function Active({ engine, width, height, uiTransform }: LoadingProps): ReactEcs.JSX.Element;
export {};
