import { IEngine } from '@dcl/ecs';
import ReactEcs, { UiTransformProps } from '@dcl/react-ecs';
interface LoadingProps {
    engine: IEngine;
    width?: number;
    height?: number;
    uiTransform?: UiTransformProps;
}
export declare function LoadingDots({ uiTransform, engine, width, height }: LoadingProps): ReactEcs.JSX.Element;
export {};
