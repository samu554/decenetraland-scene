import type { Entity } from '@dcl/ecs';
import type { EngineComponents } from './definitions';
export type FollowAction = {
    target: Entity;
    speed: number;
    minDistance: number;
    axes: {
        x: boolean;
        y: boolean;
        z: boolean;
    };
};
export declare const followMap: Map<Entity, FollowAction>;
export declare function createTransformSystem(components: EngineComponents): () => void;
